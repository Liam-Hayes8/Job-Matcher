apiVersion: v1
kind: Service
metadata:
  name: job-matcher-backend
  namespace: job-matcher
  labels:
    app: job-matcher-backend
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: job-matcher-backend